<script type="text/javascript">
    $(document).ready(function () {
        //home page
               
        $(".question").click(function () {
            var item = "";
            var id = $(this).attr("data-topass");
            var url = "/Faq/GetAnswer";
            $.getJSON(url, { Id: id }, function (data) {
                $(".ans").empty();
                $.each(data, function (i, answer) {
                    item +=
                        '<ul class=" container list-unstyled" >' +
                        '<li class="py-2">' +
                        '<i style="color: #E0A800;" class="fas fa-angle-double-right fa-lg mr-1"></i>' + answer.Text + '</li>' +
                        '</ul >';
                });
                $(".ans").html(item);
            })
        })
        //Shortcut keyword
        //$(".question").click(function () {
        //    var id = $(this).attr("data-topass");

        //    var url = "/Faq/index";
        //    $.getJSON(url, { Id: id }, function (data) {
        //        var item = "";
        //        $("#quest").empty();
        //        $.each(data, function (i, question) {
        //            item += '<div class="card-header bg-white" id="' + question.Id + '">' +
        //                '<div >' +

        //                '<h5 class="mb-0">' +
        //                ' <button id="' + question.Id + '" value="' + question.Id + '" data-topass="' + question.Id + '" class="btn  btn-xl btn-block q" style="background-color: white;text-align: left;color: #989FA5;" data-toggle="collapse" data-target="#q' + question.Id + '" role="button" aria-expanded="true" aria-controls="' + question.Id + '">' +
        //                '   <div class="d-flex">' +
        //                '  <h6 class="mr-auto"><i class="far fa-question-circle mr-2 fa-lg"></i>' + question.Text + '</h6>' +
        //                '<h6 class=""><i class="fas fa-chevron-circle-down fa-lg"></i></h6>' +
        //                ' </div>' +
        //                '</button>' +
        //                ' </h5>' +
        //                '</div >' +
        //                '</div >' +
        //                '<div id="q' + question.Id + '" class="collapse ans" aria-labelledby="' + question.id + ' " data-parent="#accordion">' +

        //                '</div >';
        //        });
        //        $("#quest").html(item);
        //        $(".q").click(function () {
        //            var id = $(this).attr("data-topass");
        //            var url = "/Faq/GetAnswer";
        //            $.getJSON(url, { Id: id }, function (data) {
        //                var item = "";
        //                $(".ans").empty();
        //                $.each(data, function (i, answer) {
        //                    item +=
        //                        '<ul class=" container list-unstyled" >' +
        //                        '<li class="py-2">' +
        //                        '<i style="color: #E0A800;" class="fas fa-angle-double-right fa-lg mr-1"></i>' + answer.Text + '</li>' +
        //                        '</ul >';

        //                });
        //                $(".ans").html(item);

        //            })
        //        })
        //    });
        //});
        //category
        //$(".cat").click(function () {
        //    var id = $(this).attr("data-topass");
        //    var url = "/Faq/GetByCategory";
        //    alert(id);
        //    $.getJSON(url, { Id: id }, function (data) {
        //        $("#quest").empty();
        //        $.each(data, function (i, question) {
        //            item += '<div class="card-header bg-white" id="' + question.Id + '">' +
        //                '<div >' +

        //                '<h5 class="mb-0">' +
        //                ' <button id="' + question.Id + '" value="' + question.Id + '" data-topass="' + question.Id + '" class="btn  btn-xl btn-block q" style="background-color: white;text-align: left;color: #989FA5;" data-toggle="collapse" data-target="#q' + question.Id + '" role="button" aria-expanded="true" aria-controls="' + question.Id + '">' +
        //                '   <div class="d-flex">' +
        //                '  <h6 class="mr-auto"><i class="far fa-question-circle mr-2 fa-lg"></i>' + question.Text + '</h6>' +
        //                '<h6 class=""><i class="fas fa-chevron-circle-down fa-lg"></i></h6>' +
        //                ' </div>' +
        //                '</button>' +
        //                ' </h5>' +
        //                '</div >' +
        //                '</div >' +
        //                '<div id="q' + question.Id + '" class="collapse ans" aria-labelledby="' + question.id + ' " data-parent="#accordion">' +

        //                '</div >';
        //        });
        //    });
        //    $("#quest").html(item);
        //})
    });
</script>
